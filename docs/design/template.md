# <Mechanism name>

Status: Research note
Date: <YYYY-MM-DD>
Deciders: <roles>
Researcher: <roles>
Purpose: <one or two sentences: what this document establishes>
Feeds into: <links to ADRs, other design docs, or topic folders>

## Flow

Plain-markdown walkthrough, numbered steps. Name the module that owns each
step (`src/localcaption/…`).

## Constants

Table of module constants with exact values (thresholds, windows, ports,
sizes, env-var names).

## Output shape

The files, JSON schemas, or artefacts this mechanism produces, with examples.

## Failure modes

What breaks, how it is signalled (exception type / exit code / warning / bool),
and what the user sees.